syntax = "proto3";
package dotbpe;

option csharp_namespace = "Survey.Core";
option optimize_for = SPEED;

// 什么字段都没有，请求
message VoidReq{

}

// 只返回默认的返回
message VoidRsp{
   string return_message = 1; //返回错误信息
}
